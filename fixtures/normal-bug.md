# Checkout button fails on Firefox

The checkout button stays disabled after all required address fields are completed in Firefox 128. Chrome works. Expected: the button becomes enabled. Actual: it remains disabled. No console errors were observed.
